{
  "name": "parcel-service-worker",
  "version": "1.0.0",
  "description": "Service worker issue reproduction",
  "main": "index.js",
  "scripts": {
    "build": "PARCEL_WORKERS=4 pnpm -r --workspace-concurrency=50% --stream run build",
    "start": "pnpm -r --stream --filter pos-portal run dev"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "parcel": "^2.8.0",
    "ts-node": "8.10.1",
    "typescript": "4.5.5"
  },
  "alias": {
    "process": {
      "global": "process"
    }
  }
}
